#ifndef CELL_H
#define CELL_H

using namespace std;

// Structure that represents a cell.
struct Cell {
  // Position in the X and Y axis.
  unsigned int x, y;

  // Constructor.  
  Cell(int _x, int _y);
};

#endif
